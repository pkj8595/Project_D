using TMPro;
using UnityEngine;

/// <summary>
/// PlayerInputController에서 실제로 요청을 받아서 처리하는 클래스 (손)
/// </summary>
public class BuildingController : MonoBehaviour
{
    public bool TryPlaceBuilding(string buildingId, Vector3 position)
    {
        return false;
    }
}
